<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
    <style>
      .product-img {
        width: 40px;
        height: 40px;
        object-fit: cover;
      }
      .table-wrapper {
        margin-top: 50px;
      }
      .form-wrapper {
        margin-top: 50px;
      }
    </style>
  </head>
  <body>
    <div class="page-wrapper">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">E-Commerce Store</a>
        <div class="navbar-nav mr-auto">
          <a class="nav-item nav-link" href="/">Products</a>
          <a class="nav-item nav-link" href="/add-product">Add Product</a>
          <a class="nav-item nav-link" href="/view-orders">View Orders</a>
          <a class="nav-item nav-link" href="/place-order">Place Order</a>
          <% if (locals.user) { %>
          <a class="nav-item nav-link" href="/account-management">Account</a>
          <% } %>
        </div>
        <div class="navbar-nav">
          <% if (locals.user) { %>
          <span class="navbar-text mr-3"> Welcome, <%= user.username %> </span>
          <a class="nav-item nav-link btn btn-outline-danger" href="/logout"
            >Logout</a
          >
          <% } else { %>
          <a class="nav-item nav-link" href="/login">Login</a>
          <% } %>
        </div>
      </nav>
    </div>
  </body>
</html>
